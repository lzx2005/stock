# AGENTS.md

本文件指导 Codex 在本仓库中的工作方式。

## 进入状态：先读 HELP.md（强制规则）

- **每个 AI 进入本仓库，第一件事必须是读 `HELP.md`**——它是给 AI 读的总目录文档：项目做什么、核心需求、5 期迭代与进度、代码结构、文档结构、待办、常用命令。读完即进入状态。
- 区分：`HELP.md` 给 AI 读（入口/进度/结构）；`README.md` 给人读（功能/上手）。不要拿 README 当 AI 入口。

## 保持 HELP.md 时刻更新（强制规则）

- **每次做完改动，必须同步更新 `HELP.md` 中对应内容**——这是硬规则，用户明确强调"这很重要"。
- 触发场景：完成一个计划 Step、改了代码结构/接口/命令、新增待办、解决待办、迭代新一期、测试数变化……都要在**同一次会话内**把 HELP.md 相应小节改到位（进度、代码结构、文档结构、常用命令、待办）。
- 判断标准：**HELP.md 永远反映仓库当前真实状态**，任何新 AI 读到它时不需要再去猜"文档是不是过时了"。宁可多更新一行，不可留下过期信息。

## 计划进度维护（强制规则）

- 本仓库用 `docs/superpowers/plans/*.md` 的多阶段实施计划跟踪开发进度，步骤用 `- [ ]`（未完成）/ `- [x]`（已完成）复选框标记。
- **每完成一个计划的 Step，必须立即把对应的 `- [ ]` 改为 `- [x]`，并在同一提交中随代码一起入库。**
- 目的：计划文件是重进会话时的进度索引。复选框维护好后，恢复工作只需读计划文件 + 跑测试，**无需重读全部代码**（省 token，这是用户明确诉求）。
- 恢复工作流程：读 `docs/superpowers/plans/` → 找第一个未勾选 Step → 从那里继续。
- 注意：以 `>` 开头、正文里举例提到"`- [ ]`"的行（如计划文件头部的说明）不是待办项，不要勾选。

## 环境

- `TICKFLOW_API_KEY` 配置在 `~/.zshrc`。非交互 shell（如本工具的 Bash）不会加载 `.zshrc`，运行涉及真实数据源的命令前先 `source ~/.zshrc`。
- 运行测试：`.venv/bin/python -m pytest tests/`（配置了 `--timeout=60`）。
- 数据在 `data/`（已 gitignore）；真实回填会写入大量数据，勿提交。
